# Copy to `docker/.env` in this folder (ignored by git) so `docker compose` injects Twitch OAuth into the server:
#   cp docker/.env.example docker/.env
# Register the same redirect URL in Twitch Developer Console:
#   http://localhost:3000/api/auth/twitch/callback
#
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
