# Copy to .env.production (local, gitignored) or set in Vercel → Environment Variables.
#
# HTTPS sites (e.g. Vercel) require a secure WebSocket URL. Browsers block ws:// from
# an https:// page (mixed content), so use wss:// only in production.
#
VITE_SIGNALING_URL=wss://your-domain.com
