# Copy to `.env.local` (gitignored):
#   cp apps/client/.env.local.example apps/client/.env.local
#
# Same backend origin for /api and WSS so `nadle_session` is sent on the WS upgrade (Mafia host, etc.).
VITE_API_URL=http://localhost:3333
VITE_SIGNALING_URL=ws://localhost:3333/ws
