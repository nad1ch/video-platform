# Dependencies (workspace hoists to repo root)
node_modules/

# Builds
dist/
dist-ssr/
build/
*.tsbuildinfo

# Env and secrets
.env
.env.*
!.env.example
!.env.production.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local reference clones (never commit)
_external/
.tmp-dict_uk/

# Hunspell sources (large); generated `words-uk-dictionary.generated.ts` is committed
apps/client/src/wordle/dictionary/raw/uk_UA.dic
apps/client/src/wordle/dictionary/raw/uk_UA.aff
apps/client/src/wordle/dictionary/raw/*.zip

# Server local DB (email/password users)
apps/server/data/

# Local / cache
*.local
.cache/

# Local-only Docker (copy from *.example.* — never commit working files)
docker/docker-compose.local.yml
apps/server/Dockerfile.local
docker/.env.local
.turbo/
.eslintcache
