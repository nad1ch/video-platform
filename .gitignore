# Dependencies (workspace hoists to repo root)
node_modules/

# Builds
dist/
dist-ssr/
build/
*.tsbuildinfo
# Legacy: server `tsc` output — build in CI/Docker, do not commit
apps/server/dist/

# Env and secrets
.env
.env.*
!.env.example
!.env.production.example
!.env.local.example
!docker/.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
# Windows reserved device name — must not exist as a file (breaks `git add`)
nul
.idea/
.vscode/*
!.vscode/extensions.json

# Local reference clones (never commit)
_external/
.tmp-dict_uk/

# Hunspell sources (large); generated `words-uk-dictionary.generated.ts` is committed
apps/client/src/nadle/dictionary/raw/uk_UA.dic
apps/client/src/nadle/dictionary/raw/uk_UA.aff
apps/client/src/nadle/dictionary/raw/*.zip

# Server local DB (email/password users)
apps/server/data/

# Local / cache
*.local
.cache/

# Local-only Docker (copy from *.example.* — never commit working files)
docker/docker-compose.local.yml
apps/server/Dockerfile.local
docker/.env.local
.turbo/
.eslintcache
